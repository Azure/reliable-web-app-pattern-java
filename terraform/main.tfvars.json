{
    "application_name": "${AZURE_ENV_NAME}",
    "database_administrator_password" : "${DATABASE_PASSWORD}",
    "enable_telemetry" : "${ENABLE_TELEMETRY=true}",
    "environment": "${APP_ENVIRONMENT}",
    "location": "${AZURE_LOCATION}",
    "location2": "${AZURE_LOCATION2}",
    "principal_id": "${AZURE_PRINCIPAL_ID}",
    "principal_type": "${PRINCIPAL_TYPE=User}",
    "proseware_client_id": "${AZURE_CLIENT_ID}",
    "proseware_client_secret": "${AZURE_CLIENT_SECRET}",
    "proseware_tenant_id": "${AZURE_TENANT_ID}"
}
