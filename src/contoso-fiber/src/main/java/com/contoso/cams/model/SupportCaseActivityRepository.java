package com.contoso.cams.model;

import org.springframework.data.jpa.repository.JpaRepository;

public interface SupportCaseActivityRepository extends JpaRepository<SupportCaseActivity, Long>{
    
}
